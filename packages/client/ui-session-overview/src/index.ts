/**
 * DSH Beacon plugin, node half. Browser behavior ships through the
 * package's `./client` entry.
 */

/** Host plugin body; this package owns no Host behavior. */
export function apply(): void {}
